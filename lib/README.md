# JDBC driver

Run from repo root:

```powershell
.\scripts\fetch-mysql-connector.ps1
```

This downloads `mysql-connector-j-8.0.33.jar` into this folder (Maven Central).
Start scripts put `lib\*` on the classpath ahead of `bin\maple.jar`.

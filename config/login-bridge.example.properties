# LoginBridge feature flags — merge into server.properties if not already present.
# Bind is always 127.0.0.1 inside LoginBridgeServer (never 0.0.0.0).
RoyMS.LoginBridge   = true
RoyMS.LoginBridgePort = 17979
